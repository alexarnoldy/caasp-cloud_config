ip a
2: eth0: <BROADCAST,MULTICAST,UP,LOWER_UP> mtu 1500 qdisc pfifo_fast state UP group default qlen 1000
    inet 172.16.241.254/24 brd 172.16.241.255 scope global eth0
3: vlan240@eth0: <BROADCAST,MULTICAST,UP,LOWER_UP> mtu 1500 qdisc noqueue state UP group default qlen 1000
    inet 172.16.240.254/24 brd 172.16.240.255 scope global vlan240

NAT setup:

Steps:
sudo yast routing ip-forwarding on; put the internal and external interfaces in their zones; set external as default; Make sure firewall reloads and starts on reboot; sudo firewall-cmd --zone=external --add-masquerade
NOTE: Seemed to need to reboot to get it to forward correctly

admin@router:~> sudo firewall-cmd --list-all --zone=external
external (active)
  interfaces: vlan240
  services: ssh
  masquerade: yes

admin@router:~> sudo firewall-cmd --list-all --zone=internal
internal (active)
  interfaces: eth0
  services: ssh mdns samba-client dhcpv6-client
  masquerade: no



